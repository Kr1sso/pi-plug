Extract 3 to 5 short search keyphrases from the conversation tail below. Each keyphrase must be 2-6 words, naming concrete entities, files, concepts, or actions worth looking up in a project knowledge wiki. Output ONLY a JSON array of strings. No prose, no fences.

Example:
["auth middleware token rotation", "PaymentService.refund retry policy", "feature flag rollout config"]

Conversation tail:
