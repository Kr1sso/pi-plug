node_modules/
*.log
.DS_Store
.pi/git/
.pi/npm/
# Don't ignore .pi/settings.json (project config) or .pi/todos
.pi/settings.json
.pi/git/
.pi/extensions/
# pi-plug's own scaffolded wiki — this is the source repo, not a project that needs a wiki
wiki/
